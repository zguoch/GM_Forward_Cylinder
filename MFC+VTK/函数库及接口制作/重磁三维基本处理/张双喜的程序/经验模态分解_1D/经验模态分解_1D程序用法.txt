一维经验模态分解程序使用说明：
（1）假若做模型试验部分，可以参考《EMD时频分析》文件夹下的程序，直接运行就可以；
（2）若对实际数据进行处理，读取数据格式为两列数据，第一列可以是时间、点数，第二列为数据，
     可在test_emd_1D.m 中读取数据，保存分解得到的数据（可根据自己的需要修改一下自己的程序），
     在emd.m中自己设置分解的阶次，达到想要的阶次，就直接结束分解，在emd.m程序中的271行修改，
     程序中的其他地方可不作修改，默认就可以。如果，连续分解彻底，可将emd.m程序中的271-274行注释掉即可！

